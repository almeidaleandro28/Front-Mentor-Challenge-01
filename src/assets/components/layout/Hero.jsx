import React from 'react';
import PropTypes from 'prop-types';

Hero.propTypes = {
  
};

function Hero(props) {
  return (
    <section className='bg-hero-mobile h-max'>
      <h2>hero</h2>
    </section>  
  );
}

export default Hero;